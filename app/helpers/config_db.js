const NAME = "localhost",
  PORT = "3000",
  DOMAIN = `http://${NAME}:${PORT}`,
  POKEMON = `${DOMAIN}/pokemon`,
  SIGNUP = `${DOMAIN}/users/register`;
export default {
  NAME,
  DOMAIN,
  POKEMON,
  SIGNUP,
};
