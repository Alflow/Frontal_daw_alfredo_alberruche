const NAME = "localhost",
  PORT = "4000",
  DOMAIN = `http://${NAME}:${PORT}`,
  POKEMON = `${DOMAIN}/pokemon`,
  ENTRENADORES = `${DOMAIN}/entrenadores`;
export default {
  NAME,
  DOMAIN,
  POKEMON,
  ENTRENADORES,
};
