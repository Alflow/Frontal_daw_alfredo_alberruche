const NAME = "localhost",
  PORT = "4000",
  DOMAIN = `http://${NAME}:${PORT}`,
  POKEMON = `${DOMAIN}/pokemon`,
  PLAYERS = `${DOMAIN}/players`;
export default {
  NAME,
  DOMAIN,
  POKEMON,
  PLAYERS,
};
